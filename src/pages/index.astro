---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import ProjectCard from '../components/ProjectCard.astro';
import PublicationCard from '../components/PublicationCard.astro';
import { getCollection } from 'astro:content';
import { SITE_TITLE, SITE_DESCRIPTION, HOME_SECTIONS } from '../consts';

// Fetch content
const projects = await getCollection('projects');
const featuredProjects = projects
	.filter((p) => p.data.featured)
	.sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
	.slice(0, 3);

const publications = await getCollection('publications');
const latestPub = publications.sort((a, b) => b.data.date.getTime() - a.data.date.getTime())[0];

const experiences = await getCollection('experience');
const currentExperiences = experiences.filter((e) => e.data.current);
---

<BaseLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
	<!-- Hero Section -->
	<Hero />

	<!-- Featured Projects Section -->
	<section class="py-10 bg-gray-50 dark:bg-dark-card/50">
		<div class="container mx-auto px-4">
			<div class="text-center mb-12" data-reveal>
				<h2 class="text-4xl font-bold dark:text-white mb-4">{HOME_SECTIONS.featuredProjects.heading}</h2>
				<p class="text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
					{HOME_SECTIONS.featuredProjects.subheading}
				</p>
			</div>

			<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
				{
					featuredProjects.map((project, i) => (
						<div data-reveal data-reveal-delay={i + 1}>
							<ProjectCard
								title={project.data.title}
								description={project.data.description}
								image={project.data.image}
								category={project.data.category}
								tags={project.data.tech}
								github={project.data.github}
								demo={project.data.demo}
								featured={true}
							/>
						</div>
					))
				}
			</div>

			<div class="text-center mt-12" data-reveal>
				<a
					href="/projects"
					class="inline-block px-8 py-3 bg-brand-blue text-white rounded-lg hover:bg-brand-blue-light transition-all hover:scale-105 shadow-lg"
				>
					{HOME_SECTIONS.featuredProjects.cta} &rarr;
				</a>
			</div>
		</div>
	</section>

	<!-- Latest Publication Section -->
	{
		latestPub && (
			<section class="py-10">
				<div class="container mx-auto px-4">
					<div class="text-center mb-12" data-reveal>
						<h2 class="text-4xl font-bold dark:text-white mb-4">
							{HOME_SECTIONS.latestResearch.heading}
						</h2>
						<p class="text-gray-600 dark:text-gray-400">{HOME_SECTIONS.latestResearch.subheading}</p>
					</div>

					<div class="max-w-4xl mx-auto" data-reveal>
						<PublicationCard
							title={latestPub.data.title}
							authors={latestPub.data.authors}
							venue={latestPub.data.venue}
							date={latestPub.data.date}
							abstract={latestPub.data.abstract}
							pdf={latestPub.data.pdf}
							doi={latestPub.data.doi}
							citation={latestPub.data.citation}
						/>
					</div>

					<div class="text-center mt-8">
						<a href="/research" class="inline-block text-brand-blue hover:underline font-semibold">
							{HOME_SECTIONS.latestResearch.cta} &rarr;
						</a>
					</div>
				</div>
			</section>
		)
	}

	<!-- Current Activities Section -->
	{
		currentExperiences.length > 0 && (
			<section class="py-10 bg-gray-50 dark:bg-dark-card/50">
				<div class="container mx-auto px-4">
					<div class="text-center mb-12" data-reveal>
						<h2 class="text-4xl font-bold dark:text-white mb-4">
							{HOME_SECTIONS.currentActivities.heading}
						</h2>
					</div>

					<div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
						{currentExperiences.map((exp, i) => (
							<div
								data-reveal
								data-reveal-delay={i + 1}
								class="bg-white dark:bg-dark-card rounded-xl p-6 shadow-lg border border-gray-200 dark:border-dark-border"
							>
								<h3 class="text-xl font-bold dark:text-white mb-2">{exp.data.title}</h3>
								<p class="text-brand-blue font-semibold mb-3">{exp.data.organization}</p>
								<p class="text-gray-600 dark:text-gray-400 text-sm">{exp.data.description}</p>
								{exp.data.tech && (
									<div class="flex flex-wrap gap-2 mt-4">
										{exp.data.tech.map((t) => (
											<span class="px-2 py-1 text-xs bg-gray-100 dark:bg-dark-surface rounded text-gray-600 dark:text-gray-400">
												{t}
											</span>
										))}
									</div>
								)}
							</div>
						))}
					</div>
				</div>
			</section>
		)
	}
</BaseLayout>
